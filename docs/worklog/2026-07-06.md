# 2026-07-06 Worklog

## Cartesian pose metric fix

- Diagnosed a false-positive Cartesian pose metric path: `episode_metric_row()` passed only
  `path.tcp_array` into `path_satisfies_constraints()`, which stripped orientations and caused
  pose-only constraints to be treated as "no clearance constraints" and therefore satisfied.
- Updated executed metrics to evaluate Cartesian pose and cylindrical passage constraints against
  the full `EpisodePath`, while keeping whole-robot clearance checks scoped to avoid/projection
  region constraints.
- Hardened executed orientation distance so quaternion-vs-rotation-matrix pose targets are scored
  instead of silently returning zero rotation error.
- Added focused regression tests for pose-not-reached false positives, robot-clearance mode, and
  rotation-matrix target orientation checks.
- Validation: `.venv/bin/python -m pytest tests/test_constrained_reach_eval.py -k cartesian_pose`
  passed, `.venv/bin/python -m pytest tests/test_constraints.py` passed, and
  `.venv/bin/python -m ruff check pg3d/eval/constrained_reach.py tests/test_constrained_reach_eval.py`
  passed. The full `tests/test_constrained_reach_eval.py` still has two unrelated failures in
  `test_eval_episode_indices_file_and_precomputed_constraints` and
  `test_constrained_eval_batch_input_inserts_goal_marker_tail_points`.

## Constrained eval trace logging

- Added executed Cartesian pose summaries to episode metrics: per-constraint minimum position
  error, best frame index, rotation error at that frame, best combined violation frame, and first
  satisfied frame.
- Added `step_traces.jsonl` to constrained eval outputs. Each row records the episode frame/step,
  selected replan/chunk step, executed policy and simulator action, TCP pose, q/agent state,
  task-goal distance, and per-frame Cartesian pose errors.
- Enriched `decisions.jsonl` with selected action chunks, selected imagined q/EEF/orientation
  rollouts, selected constraint costs, and compact per-candidate summaries so future world-model
  changes can be compared against the exact branch selected by the old run.
- Validation: `.venv/bin/python -m pytest tests/test_constrained_reach_eval.py -k "cartesian_pose or step_trace or decision_log"` passed,
  `.venv/bin/python -m pytest tests/test_constraints.py` passed, `.venv/bin/python -m ruff check pg3d/eval/constrained_reach.py pg3d/eval/__init__.py scripts/eval_constrained_reach.py tests/test_constrained_reach_eval.py` passed,
  and `git diff --check` passed. Full `.venv/bin/python -m pytest tests/test_constrained_reach_eval.py`
  is now 36 passed / 2 unrelated failures, still in
  `test_eval_episode_indices_file_and_precomputed_constraints` and
  `test_constrained_eval_batch_input_inserts_goal_marker_tail_points`.

## Dataset-derived Cartesian pose constraints

- Added `--constraint-type cartesian_pose` to `scripts/eval_constrained_reach.py` so constrained
  eval can generate one Cartesian EEF pose target from the selected dataset episode's saved zarr
  `/data/tcp_pose` trajectory instead of requiring an opaque precomputed pose JSON.
- Added `--cartesian-pose-path-fraction`, `--cartesian-pose-position-tolerance`,
  `--cartesian-pose-rotation-tolerance`, and `--cartesian-pose-weight`. The selected zarr frame is
  the exact recorded TCP pose nearest the requested arc-length fraction of the demonstration path.
- Stored source provenance metadata in generated `CartesianPoseConstraint` JSON, including dataset
  episode index, output episode index, local frame index, global frame index, episode seed, and path
  fraction.
- Updated the constrained-eval runbook with the dataset-derived pose waypoint command pattern.
- Validation: `.venv/bin/python -m pytest tests/test_constrained_reach_eval.py -k "dataset_cartesian_pose or tcp_pose_index or cartesian_pose"` passed,
  `.venv/bin/python -m pytest tests/test_constraints.py` passed, and
  `.venv/bin/python -m ruff check pg3d/constraints/programs.py scripts/eval_constrained_reach.py tests/test_constrained_reach_eval.py` passed.
  Full `.venv/bin/python -m pytest tests/test_constrained_reach_eval.py` is now 41 passed / 1
  unrelated failure in `test_constrained_eval_batch_input_inserts_goal_marker_tail_points`.
