# Worklog 2026-08-18

## Review and prepare accumulated changes

- Audited tracked and untracked repository changes before committing the E10 U-shaped obstacle
  family, visualization fixtures, non-convex benchmark plan, and focused tests.
- Restored the previously documented base diffusion-policy explanation and question notes to
  version control.
- Updated the diffusion-policy Q&A to describe the current isolated DDIM ITPS sampler rather than
  the historical DDPM implementation.
- Kept the workstation-specific xArm mesh symlink, local ITPS reference clone, and unreviewed
  experimental default score-weight changes outside the commit.

## Validation

- `.venv/bin/python -m pytest -q tests/test_obstacles.py tests/test_obstacle_collision_validation.py
  tests/test_constrained_reach_eval.py tests/test_itps_sampling.py` — 98 passed.
- Focused `ruff check` — passed.
- All new JSON fixtures passed `python -m json.tool` parsing.
- `git diff --check` — passed.

## Ten-episode U-shape placement review builder

- Added `scripts/build_u_shape_fixture.py` for deterministic, outcome-blind U placement from the
  frozen ten E3 identities. It includes locked obstacle-free nominal rollouts, fixed primary and
  fallback candidate enumeration, exact endpoint geometry gates, left/right MPLib witnesses with
  all three U components, live PhysX replay, stable goal hold, reset visibility checks, matched
  EEF/robot serialization, witness archives, top-down previews, and review-only fixture metadata.
- Added optional neutral-bundle/Rerun 0.35 inspection overlays for the nominal and witness TCP
  paths, time-indexed witness collision clouds, start/goal markers, U wireframes, clearance, and
  placement metadata.
- Added focused tests for dimension conversion, orientation, direct-back semantics, deterministic
  ordering, geometry gates, matched constraint geometry, frozen source identity, and inspection
  bundle serialization.
- Ran the exact one-episode CUDA build smoke. The locked nominal rollout succeeded stably and its
  first eight-action chunk moved 4.607 cm. All 294 primary-plus-fallback candidates failed both
  3 cm endpoint whole-arm gates, so the builder stopped as required and did not create a partial
  review fixture. The old curved-rollout back check was also corrected to evaluate the direct
  start-to-goal segment.

## Ten-episode U-shape validation

- `./.venv/bin/python scripts/build_u_shape_fixture.py --device cuda --episode-limit 1
  --max-planner-candidates 6 --config-dir /tmp/pg3d-u-review-smoke-config --artifact-dir
  /tmp/pg3d-u-review-smoke-artifacts --overwrite` — failed at the intended hard placement gate;
  no valid episode-000 candidate exists in the specified search.
- `.venv/bin/python -m pytest -q tests/test_u_shape_placement.py tests/test_dp3_reach_rollout.py
  tests/test_obstacles.py tests/test_obstacle_collision_validation.py` — 26 passed.
- `.venv/bin/python scripts/validate_obstacle_collision_geometry.py --families u_shape
  --output /tmp/e10-u-shape-collision-probe.json` — both live PhysX/SDF cases agreed.
- A synthetic inspection bundle exported through the isolated Rerun 0.35 environment and
  `.venv-rerun35/bin/rerun rrd print /tmp/e10-inspection-overlay-smoke.rrd` parsed its nominal
  path, witness path/cloud timeline, markers, summary, and clearance scalar successfully.
- Focused `ruff format`, focused `ruff check`, and `git diff --check` — passed.
