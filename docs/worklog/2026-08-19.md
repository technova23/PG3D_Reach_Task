# Work log — 2026-08-19

## Box-derived U-shape review baseline

- Replaced the slow placement-search/planner loop with a geometry-first workflow anchored to the
  ten frozen E3 boxes.
- Added `scripts/build_box_derived_u_shape_fixture.py`. It preserves each source-box center and
  initial `0.11 x 0.16 x 0.75 m` envelope, points local `-Y` toward the saved episode start, and
  writes matched EEF/robot three-box U constraints.
- Added `configs/eval/e10_u_shape_box_derived_guidance_v1.json` as the only per-episode edit surface:
  outer XY size, XY center delta, and yaw delta. This permits subsequent user-guided revisions
  without code changes.
- Extended neutral Rerun inspection bundles to support nominal-only geometry review and an optional
  static proposed-obstacle surface. Witness path/cloud/clearance fields remain all-or-none, so a
  planner-free recording cannot accidentally display or imply a cyan MPLib witness.
- Generated ten native Rerun 0.35 recordings, ten top-down PNGs, matched constraints, fixture
  metadata, and a placement report under:
  - `configs/eval/e10_u_shape_box_derived_review_v1`
  - `artifacts/e10-u-shape-box-derived-review-v1`
- The initial U mouth is 7.86 cm because the U uses the original 11 cm outer width. This is an
  intentionally literal starting point, not a claimed feasible final size. Direct start-goal paths
  miss the back in episodes 000, 006, and 007, and the saved dataset nominal misses it in most
  episodes. User-guided size/yaw revisions therefore precede MPLib.
- No ManiSkill environment, CUDA rollout, MPLib call, PhysX replay, policy visibility test,
  reranking, or ITPS comparison was run.

Commands:

```bash
./.venv/bin/python -m ruff format \
  pg3d/eval/u_shape_placement.py \
  scripts/rollout_dp3_reach_policy.py \
  scripts/export_policy_pointcloud_rerun35.py \
  scripts/build_box_derived_u_shape_fixture.py \
  tests/test_dp3_reach_rollout.py \
  tests/test_u_shape_placement.py

./.venv/bin/python -m ruff check \
  pg3d/eval/u_shape_placement.py \
  scripts/rollout_dp3_reach_policy.py \
  scripts/export_policy_pointcloud_rerun35.py \
  scripts/build_box_derived_u_shape_fixture.py \
  tests/test_dp3_reach_rollout.py \
  tests/test_u_shape_placement.py

./.venv/bin/python -m pytest -q \
  tests/test_u_shape_placement.py \
  tests/test_dp3_reach_rollout.py

./.venv/bin/python scripts/build_box_derived_u_shape_fixture.py
./.venv/bin/python scripts/build_box_derived_u_shape_fixture.py --overwrite
```

Results:

- Ruff: passed.
- Focused pytest: 25 passed in 266.23 s.
- Builder: 10/10 episodes generated; each exporter invocation passed the isolated native Rerun
  0.35 parser before the builder continued.

## First user-guided geometry revision

- Episode 000: doubled outer width from 0.11 m to 0.22 m; depth remains 0.16 m.
- Episodes 001 and 002: doubled outer width and depth to `0.22 x 0.32 m`.
- Episode 003: doubled outer width to 0.22 m, retained 0.16 m depth, and translated the center
  3 cm directly away from the start. The resulting XY delta is
  `[+0.029977366, -0.001165129] m`, and the new center is approximately
  `[-0.133012, -0.019873] m`.
- Height remains 0.75 m and yaw deltas remain zero. The four revised mouths are 15.71 cm wide;
  episodes 001 and 002 have 27.73 cm usable cavity depth.
- Regenerated all ten matched constraint pairs, top-down previews, and native Rerun 0.35 files.
  MPLib and live simulation remain intentionally deferred.

## Second user-guided geometry revision

- Episode 004: doubled outer width and depth to `0.22 x 0.32 m`.
- Episodes 005, 006, 008, and 009: doubled outer width to 0.22 m and retained 0.16 m depth.
- Episode 007: halved the baseline outer width from 0.11 m to 0.055 m and retained 0.16 m depth.
  Under the scaled-wall U definition this yields a 3.93 cm usable mouth; the literal request is
  retained for inspection without applying a hidden minimum-width clamp.
- All heights remain 0.75 m. Centers and yaw offsets are unchanged in this revision.
- Regenerated all ten matched constraints, geometry-only Reruns, top-down previews, the six-episode
  revision sheet, and the complete contact sheet. Motion-planner and live-simulator checks remain
  deferred.

## Bounded fixed-geometry MPLib validation

- Added `scripts/validate_u_shape_fixture_planner.py` with one fresh process per episode and a hard
  process-group kill after 120 s. This bounds native MPLib/setup/replay stalls in addition to
  MPLib's existing 1 s RRTConnect search budget.
- The validator never edits geometry. It tries left/right waypoint routes with 3 cm and zero MPLib
  collision margins, then applies an unchanged 3 cm exact Panda surface-cloud gate and live PhysX
  contact/stable-hold validation.
- All ten episodes were attempted and the parent continued after failures:
  - 000: hard timeout at 120.14 s;
  - 001: initial clearance 1.74 cm, below gate;
  - 002: initial penetration 1.22 cm, below gate;
  - 003: no planner route, initial clearance 7.13 cm;
  - 004: success, right route, 49 steps, 5.45 cm minimum clearance, 1.473 m TCP path;
  - 005: initial penetration 1.47 cm, below gate;
  - 006: no planner route, initial clearance 6.86 cm;
  - 007: no planner route, initial clearance 6.40 cm;
  - 008: success, left route, 53 steps, 4.50 cm minimum clearance, 0.925 m TCP path;
  - 009: success, right route, 65 steps, 3.54 cm minimum clearance, 1.331 m TCP path.
- All three successful witnesses had no contact and held success for 16 steps. Saved witness NPZs,
  native Reruns, top-down previews, logs, per-episode results, and the aggregate report are under
  `artifacts/e10-u-shape-box-derived-planner-v1`.
- No DP3 base, reranking, or ITPS comparison was run.

## Conventional direct RRTConnect test

- Added `scripts/run_conventional_u_shape_planner.py`. It plans directly from the frozen recorded
  start joint state to the successful dataset terminal joint state, with no Cartesian waypoint and
  no initial-clearance rejection. Each episode receives three 5 s RRTConnect attempts in a fresh
  worker with the same 120 s process-group timeout.
- The zero-margin run returned MPLib paths for 001, 002, 005, 008, and 009. Exact Panda
  surface-cloud grading found obstacle penetration on every returned path, so these are treated as
  sparse-point-cloud false positives rather than usable witnesses.
- Repeated the run with each U component inflated by 3 cm for planning. MPLib returned paths for
  001, 002, 008, and 009; no worker timed out:
  - 001: contact-free, 1.74 cm minimum exact clearance;
  - 002: starts with 1.22 cm penetration, exits by state 4, and then remains at least 0.35 cm clear;
  - 008: contact-free, 2.63 cm minimum exact clearance;
  - 009: contact-free, 1.84 cm minimum exact clearance.
- No direct path was returned for 000, 003, 004, 005, 006, or 007 within the bounded attempts.
  Episode 004 already has a 5.45 cm-clear live waypoint witness, so its direct-RRT failure is not an
  infeasibility result.
- None of the direct paths maintained 3 cm exact clearance for every state. Conventional outputs
  are planned and geometrically graded, not live-executed. Reports, per-episode NPZs, Reruns, and
  contact sheets are under `artifacts/e10-u-shape-conventional-planner-v1` and
  `artifacts/e10-u-shape-conventional-planner-margin3cm-v1`.

## Extended position-only conventional planning

- Increased the direct RRTConnect budget from three 5 s attempts to three 60 s attempts. The
  exact-terminal-joint run reproduced the same four paths and showed that the recorded terminal
  arm configurations collide with the U in 000, 003, 004, 006, and 007. This endpoint restriction
  was invalid for a reach task that constrains only TCP position.
- Extended `scripts/run_conventional_u_shape_planner.py` with endpoint clearance reporting,
  `dataset_pose` and `position_ik` goal modes, and configurable RRT extension range. Position-only
  mode samples 40 deterministic orientations and collects hundreds of collision-free IK goal
  configurations before RRTConnect.
- The 3 cm-inflated, three-by-60-second position-only run found paths for 000, 001, 002, 004, 005,
  006, 007, 008, and 009. Exact sampled-state grading found:
  - contact-free paths from the recorded start for 000, 001, 004, 006, 007, 008, and 009;
  - initial-collision escape paths for 002 and 005, permanently clear from states 20 and 18 with
    0.98 and 0.56 cm minimum subsequent clearance;
  - at least 3 cm clearance throughout for 004, 006, 007, 008, and 009;
  - Cartesian endpoint errors below 7 micrometres for all nine paths.
- Episode 003 retained 269 collision-free IK goal configurations but found no path across three
  independent 180 s aggregate searches: 3 cm-inflated/range 0.1, physical-size/range 0.1, and
  physical-size/range 0.03. The combined 540 s negative is strong empirical evidence but not a
  mathematical non-existence proof.
- Added a deterministic orientation-sampling unit test. All 11 focused tests pass, Ruff passes,
  and all nine position-only native Rerun 0.35 recordings parse successfully. The paths have not
  yet been replayed in live PhysX.

## U-object finalization and experiment-plan revision

- Finalized and froze the ten box-derived U-object geometries as version 1. The locked surface is
  the versioned guidance JSON, generated fixture metadata, and matched EEF/robot constraints;
  future geometry changes require a new fixture version rather than an in-place edit.
- Kept object finalization separate from environment validity. Episodes 002 and 005 remain
  invalid-start diagnostics, episode 003 remains an unresolved-route diagnostic, and episodes
  000, 001, 004, 006, 007, 008, and 009 form the predeclared witnessed valid-start stratum.
- Replaced the outdated broad non-convex plan with the current comparison: whole-body ITPS versus
  exact world-model continuation reranking at planning horizon 3 and execution horizon 1. The plan
  explicitly avoids calling the current continuation mechanism true beam search.
- Deferred the long-wall, false-passage fork, chicane, hook/C, elbow-trap, and doorway/shelf scene
  families for later implementation and independent versioned finalization.
- Regenerated all ten geometry fixtures and Rerun inspection artifacts from the finalized guidance;
  the fixture now records `object_geometry_finalized` without claiming that planner, live-PhysX,
  or comparison results are embedded in the review artifact.
- Focused Ruff format/check passed, all generated fixture JSON parsed, `git diff --check` passed,
  and the focused U-placement/Rerun suite passed with 27 tests.

## DDIM eta for U-scene candidate diversity

- Added an explicit `--ddim-eta` evaluator setting and locked the U-scene H3/E1 reranking run to
  `1.0`, adding reverse-process variance on top of independently sampled initial diffusion noise.
- Passed eta and the seeded generator into ordinary DDIM scheduler steps and serialized the value
  in method rows, summaries, and W&B configuration.
- Kept the default at `0.0` so frozen historical protocols do not change implicitly. ITPS retains
  its isolated released eta-0 DDIM transition even when ordinary reranking uses eta 1.
