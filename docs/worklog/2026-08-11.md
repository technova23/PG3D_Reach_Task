# Work log — 2026-08-11

## Freeze the canonical 75 cm candidate-midpath suite

- Promoted the ten accepted placements from
  `artifacts/candidate-midpath-clearance-safe-100k-pilot` into the tracked fixture
  `configs/eval/e3_candidate_midpath_75cm_frozen_v1`.
- Froze output ordering, dataset episode indices, simulator and policy seeds,
  source-pool and placement-policy seeds, measured initial robot clearances, and
  exact obstacle centers.
- Added matched EEF-target and robot-target constraint directories. Both use the
  same grounded 75 cm axis-aligned carton geometry; only the planner-guidance body
  differs.
- Added the suite to `configs/eval/e3_episode_split.json` without changing the
  locked checkpoint-selection, pilot, or definitive-test partitions.
- Recorded the durable decision in ADR 0010 and added runbook commands that consume
  the frozen files instead of regenerating placements.
- Added a regression test that checks episode order, split isolation, minimum
  initial clearance, target-specific constraint loading, and exact geometry parity.

Validation:

- Frozen episode-index file and all ten robot constraint geometries match the
  historical clearance-safe source artifacts.
- Focused fixture and split tests: 2 passed.
- Full constraint and constrained-evaluation suite: 102 passed.
- Ruff on the affected test module: passed.
- All fixture/split JSON parsed with `jq`; `git diff --check`: passed.
