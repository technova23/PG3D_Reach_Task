# Worklog — 2026-08-21

- Added `itps_reranking` and `itps_beam` to the canonical constrained-reach evaluator.
- Refactored standalone ITPS through a shared independent-candidate proposal helper and added
  deterministic ancestry-based seed metadata.
- Reused exact world-model rollout and beam-prefix verification, with feasible-first selection and
  max-violation/integral/distance fallback ordering.
- Added matched-budget method records, decision/Rerun diagnostics, and focused seed, selection, and
  beam regression tests.
- Validation: Python compilation, Ruff, and six focused pytest tests passed. `uv run` is currently
  blocked by the existing `cu129` source/optional-dependency metadata mismatch; live CUDA,
  ManiSkill, MP4, Rerun, and six-episode experiment runs were not performed in this task.
- Corrected extended-horizon evaluation so `--max-steps` overrides the dataset's saved Gym
  `max_episode_steps` rather than being preempted at 150. Added `itps_beam` episode totals and
  per-replan metrics for expanded, feasible, and retained nodes, with focused regression tests.
- Audited the max-300 hybrid results and fixed the consecutive hold state machine. The old counter
  advanced after first success even through failed frames, producing `stable_success` termination
  alongside `stable_goal_reached=false`. Failed frames now reset the streak, and stable distance
  grading searches for a complete later hold after recovery. Historical episode 001/009 stable
  labels are not valid; their minimum-distance and reach-once metrics remain usable.
- Replaced the transitional simulator-success counter with explicit distance-based hold telemetry:
  `current_hold_count`, `maximum_hold_count`, `first_goal_entry_step`, and `stable_entry_step`.
  Stable termination now uses the same 2.5 cm TCP threshold as episode grading, so a drifted trial
  continues toward the full task horizon instead of stopping on elapsed post-entry time.
- Froze the six-fixture development manifest and full versioned ITPS-beam protocol, with exact
  dataset indices, simulator/policy seeds, constraint hashes, fixed physical thresholds, grids,
  lineages, budgets, and output schemas.
- Added normalized feasible-prefix task scoring and exact infeasible fallback ordering, preserving
  historical avoidance-only scoring as a named ablation.
- Added independently replayable guided proposals: explicit initial/inner ITPS noise seeds,
  deduplicated conditioning and guidance-geometry NPZ bundles, action hashes, full proposal traces,
  direct proposal replay, one-controller replay comparison, and offline beam-prune audit commands.
- Added all-expanded-node telemetry plus imagined-versus-executed joint/TCP/clearance validation
  and a non-overwriting 95th-percentile verifier-buffer calibration command with the 3 cm stop gate.
- Added beta-binomial feasible-mass scoring, one initial probe for every feasible node, the global
  20-proposal cap, mean/LCB ablations, projected adaptive weights, uncertainty allocation, width
  1--2 contraction, and deterministic 16-point route diversity.
- Added the config-driven staged launcher and development-only reports. Focused CPU validation
  passed; CUDA/ManiSkill experiment execution remains pending and no six-fixture result from this
  implementation is claimed here.
