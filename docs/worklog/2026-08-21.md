# Worklog — 2026-08-21

- Added `itps_reranking` and `itps_beam` to the canonical constrained-reach evaluator.
- Refactored standalone ITPS through a shared independent-candidate proposal helper and added
  deterministic ancestry-based seed metadata.
- Reused exact world-model rollout and beam-prefix verification, with feasible-first selection and
  max-violation/integral/distance fallback ordering.
- Added matched-budget method records, decision/Rerun diagnostics, and focused seed, selection, and
  beam regression tests.
- Validation: Python compilation, Ruff, and six focused pytest tests passed. `uv run` is currently
  blocked by the existing `cu129` source/optional-dependency metadata mismatch; live CUDA,
  ManiSkill, MP4, Rerun, and six-episode experiment runs were not performed in this task.
- Corrected extended-horizon evaluation so `--max-steps` overrides the dataset's saved Gym
  `max_episode_steps` rather than being preempted at 150. Added `itps_beam` episode totals and
  per-replan metrics for expanded, feasible, and retained nodes, with focused regression tests.
