# Work log — 2026-07-22

## Consolidate the active DP3 module implementation

- Confirmed that policy construction and training import architecture components exclusively from
  `pg3d.policies.dp3.modules`.
- Removed the unused `pg3d/policies/modules.py` and misspelled
  `pg3d/policies/mudules.py` legacy copies. They had diverged from the active implementation and
  did not contain the checkpoint's cross-attention and goal-encoder path.
- Kept `pg3d/policies/dp3/modules.py` unchanged as the single canonical implementation for the
  base policy and the ITPS branch.
- This cleanup intentionally precedes the planned line-referenced explanation of base diffusion
  policy training and inference.

Validation:

- `.venv/bin/pytest -q`: 149 passed.
- `.venv/bin/python scripts/smoke_imports.py`: passed.
- `git diff --check`: passed.
- Runtime import scan found no references to either removed module.
- Repository-wide Ruff remains blocked by 217 pre-existing findings in unrelated dataset,
  experimental, and untracked clone files. A focused active-DP3 lint also reports two existing
  formatting findings in files left unchanged by this cleanup.

## Document ordinary diffusion-policy behavior

- Added `docs/explainations/diffusion_policy.md` with exact source anchors for dataset sequence
  construction, normalization, observation encoding, forward diffusion, epsilon-prediction
  training, EMA updates, ordinary DDIM inference, action slicing, and direct base execution.
- Separated generic code defaults from the inspected configuration stored in
  `/scratch2/skills/train_final_Arya/step_00065000.pt`.
- Reserved the final section for a later line-referenced ITPS explanation.
- Verified all 55 local Python source links resolve, `git diff --check` passes, and the focused DP3
  policy/dataset/training/rollout/import suite passes with 36 tests.

## Answer diffusion-scheduler questions

- Added `docs/explainations/diffusion_policy_questions.md` explaining how independent initial
  Gaussian samples produce multiple candidates under deterministic DDIM.
- Documented why the epsilon predictor can be sampled with DDPM after training through the shared
  DDIM/DDPM forward-noising process.
- Recorded the current RNG distinction: ordinary candidate sampling relies on global PyTorch RNG,
  while ITPS explicitly threads its generator through initial, reverse, and re-noising operations.
- Extended the diffusion-policy Q&A with a line-by-line explanation of `conditional_sample`,
  Boolean conditioning masks, per-step clamping, and why clamping is a no-op for the current
  globally conditioned checkpoint.
- Clarified that global conditioning can use cross-attention or FiLM, and that the alternative
  inpainting configuration generates unmasked future observation features as well as actions.
- Documented the checkpoint's split point-cloud encoding: 832 scene points through PointNet and
  192 ordered synthetic goal points through a separate marker MLP, plus explicit goal-relative and
  robot-state conditioning.
