# Worklog 2026-07-20

## Faithful ITPS stochastic sampling

- Replaced the provisional direct trajectory-gradient loop with the released ITPS annealed-MCMC
  transition. Intermediate inner steps now re-noise the predicted clean sample at the same noise
  level; only the final inner step advances the DDPM chain.
- Added isolated scheduler construction, complete generator plumbing, epsilon-prediction checks,
  batched energy validation, timestep-zero behavior, and the normal DP3 action-slice wrapper.
- Reimplemented differentiable Panda FK from ManiSkill's `panda_v2.urdf`, including fixed hand/TCP
  transforms and the live world-from-base transform. The required simulator comparison covered the
  rest pose plus ten sampled configurations and measured `0.00000061 m` maximum error.
- Added differentiable multi-region EEF avoidance for spheres, boxes, and XY projected rectangles.
  Smooth softplus and exact hinge energies both respect constraint margin and weight.
- Integrated ITPS into `eval_constrained_reach.py` with released defaults (`guide_ratio=60`,
  `mcmc_steps=4`) and removed the incomplete standalone diagnostic.
- Repaired stale branch tests to reflect structured goal-marker offsets, metadata-derived marker
  defaults, current reach-writer arguments, import isolation, and the previously missing real/sim
  synchronization helper.

Commits:

- `03771da Implement faithful ITPS stochastic sampler`
- `9a1acf5 Align differentiable Panda FK with ManiSkill`
- `a68bdb3 Integrate ITPS avoidance guidance`

Validation:

- `UV_CACHE_DIR=/tmp/pg3d-itps-final-uv-cache UV_NO_SYNC=1 make test` — 149 passed.
- `UV_CACHE_DIR=/tmp/pg3d-itps-final-uv-cache UV_NO_SYNC=1 make smoke` — passed.
- `.venv/bin/python scripts/check_panda_fk.py --samples 10 --tolerance 0.001` — passed outside
  the sandbox with `0.00000061 m` maximum error.
- Focused Ruff checks over every ITPS/FK/evaluator file — passed.
- `make lint` remains blocked by 223 pre-existing Ruff findings in unrelated experimental and
  legacy files, including dataset-generation, xArm, visualization, and the untracked ITPS clone.
