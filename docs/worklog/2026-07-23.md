# Work log — 2026-07-23

## Define the ITPS-versus-reranking experiment protocol

- Added `docs/explainations/itps_reranking_experiment_plan.md`.
- Fixed stable combined task-and-constraint success as the primary paper outcome,
  retained ordinary combined success for interim pilots, and distinguished transient
  goal reach from stable goal holding.
- Defined task, constraint, motion, compute, and method-specific diagnostic metrics
  using executed ManiSkill trajectories as the common measurement source.
- Specified paired episodes, confidence intervals, paired method comparisons, and the
  separation between full-distribution and nominal-base-success-subset results.
- Proposed the initial experiment ladder: metric validation, nominal checkpoint gate,
  primary comparison, constraint difficulty, compute matching, method ablations,
  geometry targets, robustness, and world-model prediction quality.
- Audited the definitions against the current constrained-reach evaluator and listed
  the paper-scale metrics that are not yet logged.
- Added a required realistic-obstacle experiment before the primary comparison.
  Simulator actors must contribute naturally camera-visible points to the policy
  observation, while their known pose and collision primitives provide the identical
  constraint geometry used by reranking and ITPS. Point-cloud boundary inference is
  explicitly deferred to a later perception ablation.
- Added obstacle families, point-retention checks through cropping/goal-token
  insertion, world-model static-scene checks, geometry/render synchronization checks,
  and obstacle-specific logging fields.
- Made qualitative outputs a required experiment deliverable. Every reported run must
  produce paired simulator MP4 and Rerun `.rrd` artifacts for a deterministic,
  method-matched episode subset, plus an artifact manifest linking videos, point-cloud
  timelines, metrics, constraints, obstacle configs, checkpoint, and git commit.
- Defined the required Rerun contents: policy-visible point clouds with semantic
  categories, executed and candidate trajectories, obstacle collision geometry,
  clearance state, ITPS/reranking diagnostics, and imagined-versus-executed world-model
  overlays where applicable.

Validation:

- Documentation-only change; no runtime tests were required.
- `git diff --check` passed.

## Implement E0 constrained-reach metric foundations

- Added stable goal-hold and stable combined success using the configured goal
  threshold and post-success hold window.
- Added per-step TCP clearance reduction plus violation depth, duration, fraction,
  time integral, and event-count metrics.
- Added TCP/joint path length, physical-time acceleration and jerk, and maximum joint
  velocity using the live simulator control timestep.
- Extended aggregate summaries with medians and quartiles while retaining existing
  mean/standard-deviation and compatibility fields.
- Threaded the resolved goal threshold, hold steps, and ManiSkill control timestep
  through the live constrained evaluator.
- Added synthetic tests for incomplete holds, drift after success, multiple violation
  events, physical timestep scaling, and episode-row integration.

Validation:

- `.venv/bin/pytest -q`: 153 passed.
- Focused Ruff check for the changed evaluator, metric module, exports, and tests:
  passed.
- `git diff --check`: passed.

## Make visual artifacts an experiment completion gate

- Clarified that graphs and tables alone do not complete E3--E9.
- Required a matched playable MP4 and readable, time-synchronized Rerun `.rrd` for
  every selected method/episode, linked to the exact metrics row by the artifact
  manifest.
- Required at least one deterministic qualitative suite for every major experiment,
  while allowing predeclared representative subsets for large sweeps.

## Add measured compute-operation metrics

- Added a denoiser forward hook that records actual module calls and batch-item
  equivalents without changing the DP3 or ITPS samplers.
- Counted fast EEF queries, exact robot-cloud queries/cache-miss renders, and
  vectorized ITPS FK calls plus evaluated poses only within action selection.
- Added per-episode/per-replan geometry totals and absolute/incremental peak PyTorch
  CUDA allocation. Rerun and post-hoc whole-robot grading are excluded.
- Fixed the exact single-chunk evaluator path, which passed unsupported directional
  arguments to the shared composition controller.
- A live one-replan `K=2` smoke measured base at 100 denoiser calls/100 sample
  evaluations, reranking at 100/200 plus 16 EEF queries, and ITPS at 400/400 plus
  396 differentiable FK calls over 6,336 poses. Peak allocation was about 759 MB for
  base/reranking and 1.49 GB for ITPS.
- A separate exact `K=1` reranking smoke measured eight EEF queries, eight cloud
  queries, and eight cache-miss renders.

Validation:

- Full test suite: 193 passed; focused constrained-eval and geometry tests: 60
  passed.
- Focused Ruff checks and `git diff --check`: passed.
- Live fast and exact CUDA/ManiSkill smokes completed and populated `metrics.jsonl`
  plus summary aggregates.

## Add time-indexed robot safety and action discontinuity

- Replaced flattened whole-robot clearance aggregation with one mesh-derived robot
  point cloud per sampled executed timestep. The default stride is now one for a true
  time-indexed paper metric.
- Added primary whole-robot violation depth, duration, fraction, integral, and event
  count while retaining all TCP-only diagnostics under `_tcp` keys.
- Recorded executed simulator arm targets and report mean/maximum discontinuity over
  all transitions plus a separate mean/maximum over replan boundaries.
- A nine-step, two-replan live smoke at
  `/tmp/pg3d-motion-robot-metrics-smoke` reported whole-robot clearance `0.0497` m,
  zero violation steps, overall discontinuity mean `0.0305`, and boundary
  discontinuity `0.0462`.

## Add validated qualitative-artifact manifests

- Added `pg3d.artifact_manifest.v1` generation to constrained evaluation.
- Each selected method/episode record links its MP4, `.rrd`, and constraint JSON to
  the exact metrics-row index, method, episode, simulator/policy seeds, dataset
  episode, constraint fingerprint, obstacle geometry, checkpoint, dataset, and git
  commit.
- Required files must exist and be non-empty; sizes and SHA-256 digests are recorded
  and validated. Duplicate entries and identity mismatches fail the run.
- Added `git_commit`, `method_config`, and `artifact_manifest_path` to episode rows.
- Enforced the qualitative contract at the CLI boundary: `--video` requires
  `--rerun`, and selected video indices must be a subset of selected Rerun indices.
- A live cylinder smoke at `/tmp/pg3d-artifact-manifest-smoke` produced one manifest
  entry; all three file digests were independently recomputed and matched.

## Add semantic planning timelines to Rerun

- Rerun now records the exact fixed-size point tensor supplied to DP3 after
  goal-marker insertion. Separate entities identify valid robot, ordinary-scene,
  obstacle, and goal-marker points.
- Added executed TCP path history, target/constraint geometry, and per-step signed
  clearance plus binary violation state.
- Reranking recordings contain every imagined candidate EEF path, feasibility color,
  score, and the selected path. Base and ITPS recordings contain their selected
  predicted paths; ITPS's path is the final guided sample.
- Added tests proving the Rerun semantic cloud is numerically identical to the policy
  tensor and that goal-marker replacement clears overwritten obstacle labels.
- Ran base/reranking/ITPS at `/tmp/pg3d-rich-rerun-smoke-v2`. All three produced
  MP4/`.rrd` pairs and manifest entries. `rerun rrd print` confirmed the semantic,
  clearance, candidate, score, selected-path, and executed-path entities in the
  reranking recording.

## Start E2 with a camera-visible collidable box

- Added `--embody-obstacle` for a generated single `BoxRegion`. The Panda reach
  environment builds the box as a collidable kinematic control-environment actor and
  places it at the constraint center during every paired method reset.
- Added a runtime invariant that the actor half-extents exactly match the serialized
  scoring geometry. Unsupported shapes, multiple regions, and precomputed geometry are
  rejected instead of silently producing mismatched actors and constraints.
- Kept the live simulator camera observation for dataset-seeded embodied-obstacle
  episodes; the prior dataset point cloud is obstacle-free and must not overwrite it.
- Propagated the obstacle segmentation mask through fixed-count cropping, logged raw,
  cropped, and final policy-input counts, and colored obstacle points separately in
  Rerun.
- The one-step GPU smoke at `/tmp/pg3d-e2-box-smoke-v2` produced a non-empty MP4 and
  `.rrd`. The actor is visibly present and collidable. Counts were 192 raw points, 5
  cropped points, and 5 final policy-input points.
- The low retained count establishes the next E2 requirement: semantic-preserving
  obstacle sampling is necessary before using embodied obstacles in E3.

## Preserve obstacle points during downsampling

- Added a configurable `obstacle_point_quota` to the shared point-cloud crop config
  and `--obstacle-point-quota` to constrained evaluation (default 32).
- The sampler reserves visible obstacle points first, then satisfies the existing
  robot quota and fills remaining ordinary-scene slots without changing the tensor
  size. The quota is a minimum; ordinary-scene filling may retain additional
  obstacle points.
- Repeated the same one-step smoke at `/tmp/pg3d-e2-box-quota-smoke`. From 192 raw
  obstacle points, 36 survived cropping and all 36 remained in the final policy
  tensor, versus 5 before the quota. Non-empty MP4 and `.rrd` artifacts were retained.

## Add synchronized rotated-box geometry

- Extended `BoxRegion` with a world-frame yaw and implemented the same local-frame
  signed-distance transform in NumPy and differentiable Torch.
- Applied the serialized yaw to the collidable control actor, overlay actor, and
  Rerun wireframe. Robot-clearance-aware placement now preserves orientation.
- Added `--obstacle-yaw-deg` and exercised a 35-degree box at
  `/tmp/pg3d-e2-rotated-box-smoke`.
- The serialized yaw was 0.610865 radians; 153 raw obstacle points were visible and
  36 survived both cropping and final policy-input construction. The MP4 and `.rrd`
  were non-empty, and the decoded video frame was visually inspected.

Validation:

- Rotated NumPy SDF, JSON round-trip, Torch energy, and wireframe tests: passed.
- Focused test suite: 80 passed.
- One-step CUDA/ManiSkill rotated-box smoke: passed.

## Add the tall-carton obstacle family

- Added a named `carton` family with predeclared half-extents
  `[0.055, 0.08, 0.16]` metres and a distinct cardboard material. Explicit
  half-extents remain available as an override.
- Added required episode fields for embodied obstacles: `obstacle_id`,
  `obstacle_family`, `obstacle_pose`, and `obstacle_collision_geometry`.
- Ran a 20-degree carton smoke at `/tmp/pg3d-e2-carton-smoke`. It observed 457 raw
  obstacle points, retained 46 after cropping, and retained all 46 in the final DP3
  input. Its MP4 and `.rrd` were non-empty, and the decoded frame was inspected.

## Add matched finite-cylinder geometry

- Added a serialized finite `CylinderRegion` with NumPy and differentiable Torch
  signed-distance implementations plus Rerun wireframe support.
- Added a named `cylinder` actor family with radius `0.055` m and half-length
  `0.12` m.
- The first rendered smoke revealed that ManiSkill's cylinder primitive is X-axis
  aligned while `CylinderRegion` is Z-axis aligned. Rotated the collidable actor so
  +X maps to +Z and reran rather than accepting mismatched visual/collision geometry.
- The corrected smoke at `/tmp/pg3d-e2-cylinder-smoke-v2` visibly showed a vertical
  cylinder, observed 218 raw points, and retained 36 after cropping and in the final
  DP3 input. Its MP4 and `.rrd` were non-empty.

## Add the composite open-cabinet family

- Defined a reusable cabinet model containing left/right sides, top, bottom, back,
  shelf, and an open door rotated 70 degrees around its hinge.
- Built all seven components as collidable control actors and expanded the single
  placement root into seven identically transformed serialized `BoxRegion`
  constraints. Composite segmentation IDs are merged into one obstacle mask.
- Ran a 10-degree cabinet smoke with a 64-point minimum at
  `/tmp/pg3d-e2-cabinet-smoke`. It observed 768 raw points and retained 92 in both
  the crop and final policy tensor.
- The metrics contain seven collision primitives, the MP4 and `.rrd` are non-empty,
  and the decoded frame visibly shows the body, shelf, open front, and door.

## Complete the E2 collision-geometry gate

- Added `scripts/validate_obstacle_collision_geometry.py`, which creates a dynamic
  spherical collision probe during scene construction and compares raw PhysX contact
  pairs with serialized signed clearance.
- The initial center-contained probe and force-threshold approaches were rejected:
  fully contained convex shapes need not generate a surface contact, and valid
  contacts can have near-zero instantaneous force. The final cases deliberately cross
  an exterior surface and use contact-pair identity for the boolean decision.
- Ran intersecting and separated cases for box, carton, vertical cylinder, and
  composite cabinet. All eight cases agreed; results are stored at
  `/tmp/pg3d-e2-obstacle-collision-validation-v5.json`.
- E2 is now complete: every planned family has synchronized actors/constraints,
  ordinary-camera policy points, semantic quota validation, MP4/`.rrd` evidence, and
  collision-versus-clearance agreement.

Validation:

- Focused unit tests: 72 passed.
- One-step CUDA/ManiSkill embodied-box smoke: passed.
- The saved MP4 was decoded and visually inspected; the brown box is present in the
  control-environment render.

## Complete E0 paired protocol and timing validation

- Added a stable SHA-256 fingerprint over serialized constraint geometry and wrote it
  into every episode metric row.
- Added order-independent per-episode policy seeds. Every paired method now resets
  Torch and NumPy sampling to the same recorded seed before action selection, so method
  ordering cannot silently change later results.
- Added run, checkpoint, source, dataset-episode, simulator-seed, policy-seed,
  constraint-ID, and constraint-path fields to episode rows.
- Added a fail-closed paired-protocol validator that rejects missing/duplicate methods
  or mismatched simulator, policy, constraint, source, and checkpoint identities.
- Added a CUDA-synchronized action-selection timing boundary around the complete
  observation-to-selected-chunk operation, with episode total, median, p90, and p95.
- Added unit coverage for constraint fingerprint round trips, seed derivation,
  mismatched pair rejection, and latency aggregation.

Validation:

- `.venv/bin/pytest -q`: 156 passed.
- Focused Ruff check for the changed evaluator, metric module, exports, and tests:
  passed.
- `git diff --check`: passed.
- Live paired ManiSkill smoke passed on dataset episode `0` / simulator seed
  `48572821` with checkpoint `/scratch2/skills/train_final_Arya/step_00065000.pt`:
  `base`, `rejection`, `reranking`, and `itps` all completed one executed step with
  shared policy seed `916202559` and constraint fingerprint
  `e3800225677a25f9df1dea00136d376fed9b2842685a243eb794f19cc88905e6`.
- The synchronized smoke recorded complete action-selection latency for all four
  methods and wrote valid `metrics.jsonl`, `decisions.jsonl`, `timings.jsonl`,
  constraint JSON, and `summary.json` under `/tmp/pg3d-e0-paired-smoke-v3`.

## Complete E1 nominal checkpoint gate

- Added an explicit `--no-constraints` evaluator mode that writes and fingerprints an
  empty constraint program instead of approximating nominal evaluation with a distant
  or zero-weight obstacle.
- Fixed deterministic video/Rerun selection: enabling video now records only the
  selected episode subset instead of silently recording every episode.
- Fixed post-success evaluation semantics. ManiSkill success termination no longer
  prevents hold collection, and the hold window extends beyond rather than consumes
  the fixed nominal task-step budget.
- Ran the 65k checkpoint on 25 unique held-out dataset seeds with an 80-step task
  horizon and 16-step hold. It reached 14/25 (56%, Wilson 95% CI 37.1–73.3%) and
  stably held 12/25 (48%, Wilson 95% CI 30.0–66.5%), missing the predeclared 15/25
  interpretation gate by one.
- Wrote five deterministic MP4s and five matching Rerun `.rrd` files under
  `artifacts/e1-nominal-step-65000-v2`, including four successful episodes and one
  failure in the preselected subset.
- Ran a three-episode, eight-step `K=1` zero-guidance regression across base,
  rejection, reranking, and ITPS. Base/rejection/reranking selected chunks were
  bit-identical on every seed. ITPS differed, as expected, because guide ratio zero
  removes the energy gradient but does not replace its DDPM/MCMC sampler with DDIM.
- The regression wrote 12 non-empty MP4s and 12 non-empty `.rrd` files under
  `artifacts/e1-zero-guidance-step-65000`.

Validation:

- `.venv/bin/pytest -q`: 161 passed.
- Focused Ruff check for the evaluator and constrained-eval tests: passed.
- `git diff --check`: passed.
