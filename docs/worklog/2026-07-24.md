# Work log — 2026-07-24

## Stop videos on visible contact and preserve camera pixels

- Audited the interrupted definitive v2 directory. It contains 11 partial rows:
  episodes 0--1 are paired, and episode 2 lacks ITPS. It is not an accepted result.
- Confirmed the reported video issue in episode 2: whole-robot clearance reached
  approximately `-0.001 m`, but PhysX emitted no named contact pair, so the rollout
  continued to 150 steps.
- Changed embodied-obstacle termination to the union of PhysX contact and online
  non-positive whole-robot signed clearance. The same per-step robot clouds are
  reused for final whole-robot grading rather than rendered a second time.
- Added explicit geometric/union contact step, source, and clearance fields. The
  contact frame is retained and no later simulator step is executed.
- Moved the mandatory MP4 identity/outcome text to a new header above the camera
  frame, preserving every original simulator pixel.
- Bumped definitive output paths to protocol v3 so neither partial v1 nor partial v2
  artifacts can be confused with accepted results.

Validation:

- Focused constrained-evaluation tests: 79 passed.
- Three-episode live CUDA smoke: the known shallow-contact episode stopped at step
  70 with 71 frames and `obstacle_contact_source=geometry`; the old run continued to
  step 150 with 151 frames.
- All three smoke MP4s decoded at exactly 512x576 (64-pixel header plus untouched
  512x512 camera image), and all three matching native Rerun files opened.
