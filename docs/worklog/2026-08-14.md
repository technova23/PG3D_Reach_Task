# Worklog 2026-08-14

## Switch ITPS reverse scheduler to DDIM

- Changed the isolated ITPS reverse scheduler from DDPM to deterministic DDIM while retaining
  fresh Gaussian re-noising during the first three annealed-MCMC inner steps.
- Added the `ddim` scheduler identity to ITPS evaluation metadata so new runs remain distinguishable
  from historical DDPM-ITPS artifacts.
- Updated the focused scheduler test, status, experiment plan, and runbook. Existing constrained
  results were generated with the old DDPM scheduler and require rerunning before comparison with
  the new implementation.

## Validation

- `.venv/bin/python -m pytest -q tests/test_itps_sampling.py` — 6 passed.
- `.venv/bin/python -m pytest -q tests/test_itps_guidance.py` — 7 passed.
- `.venv/bin/python -m pytest -q tests/test_constrained_reach_eval.py` — 86 passed.
- `.venv/bin/ruff check pg3d/policies/dp3/policy.py scripts/eval_constrained_reach.py tests/test_itps_sampling.py` — passed.
- `git diff --check` — passed.

## Add ITPS whole-body guidance

- Added differentiable transforms for Panda link1--link7, the hand, and both fingers while keeping
  the existing TCP FK on the same chain.
- Added deterministic startup sampling of 1024 movable-link collision-surface points from the
  active ManiSkill Panda URDF.
- Extended Torch avoidance energy and ITPS inference to support robot-target constraints with an
  exact worst-point reduction across the horizon and sampled geometry.
- Kept fixed link0 out of gradient guidance while retaining the existing full-robot initial and
  executed collision checks.
- Added collision-point compute counters and compressed Rerun rollouts with link colors and
  per-constraint worst points.

## Whole-body guidance validation

- `.venv/bin/python -m pytest tests/test_panda_fk.py tests/test_panda_collision.py -q` — passed.
- `.venv/bin/python -m pytest tests/test_itps_guidance.py tests/test_itps_sampling.py -q` — passed.
- `.venv/bin/python -m pytest tests/test_constrained_reach_eval.py tests/test_dp3_reach_rollout.py -q` — passed.
- Focused `ruff check` and `git diff --check` — passed.
- The live ManiSkill FK and GPU evaluation checks remain pending because this execution environment
  exposes no supported CUDA/render device.

## Plan non-convex and delayed-consequence experiments

- Added `docs/explainations/nonconvex_scene_experiment_plan.md` as the durable follow-on plan after
  the whole-body ITPS box baseline.
- Prioritized U-shaped, distant-opening, false-fork, staggered-gate, elbow-trap, and shelf/overhang
  scenes by the local-minimum or delayed-decision failure mechanism they test.
- Defined separate ablations for obstacle-visible versus obstacle-free ghost policy inputs,
  multi-chunk continuation, and true multi-depth beam search.
- Added scene validity rules, parameter sweeps, candidate-support checks, interpretation rules,
  metrics, artifact requirements, and a definitive-evaluation completion gate.
- Linked the plan as E10 from the main ITPS-versus-reranking protocol without modifying frozen E3.

## Documentation validation

- `git diff --check` — passed.

## Add and visualize the first E10 U-shaped scene

- Added a scalable `u_shape` obstacle family composed of two side walls and one closed back, with
  matched ManiSkill actors and serialized `BoxRegion` constraints.
- Added the clearance-safe visualization fixture `configs/eval/e10_u_shape_smoke_v1` for selected
  dataset episode 305. The opening faces the start and the direct start-goal path crosses the back.
- Generated `artifacts/e10-u-shape-smoke-v1-visualization` with an MP4, native Rerun timeline,
  exact policy-input bundle, manifest, and top-down preview. The final DP3 tensor retained 106
  obstacle points.
- Added U-shape coverage to the simulator-contact validator and documented the reproduction command
  in the runbook.

## U-shape validation

- `.venv/bin/python -m pytest -q tests/test_obstacles.py tests/test_obstacle_collision_validation.py tests/test_constrained_reach_eval.py` — 92 passed.
- Focused `ruff check` — passed.
- `scripts/validate_obstacle_collision_geometry.py --families u_shape` — two cases, zero failures.
- `git diff --check` — passed.

## Move the U-shape visualization to episode 004

- Added `configs/eval/e10_u_shape_smoke_episode004_v1` for selected output episode 004 / dataset
  episode 1010 while retaining the original episode-000 smoke as provenance.
- Reoriented the unchanged `0.28 x 0.30 x 0.60 m` U so its opening faces the episode-004 start and
  its closed back crosses the nominal route toward the goal.
- Initial whole-robot clearance increased from 3.23 cm in episode 000 to 7.91 cm in episode 004.
- Generated `artifacts/e10-u-shape-episode004-visualization`; 171 obstacle points reached the final
  policy tensor, and the eight-step visualization remained collision-free with 4.40 cm minimum
  executed clearance.
